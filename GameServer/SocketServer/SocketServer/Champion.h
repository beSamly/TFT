#pragma once
#include "ClientSession.h"

class Champion
{
public:
    Champion(){};
    int star;
    void Update(float deltaTime);
    // TransformController
    // StatController
    // SkillController
};
